_schema-version: "3.1"
ID: geo-weather-agent
version: 1.0.0
description: "Geo Weather Agent - assesses geopolitical and weather risks for routes"

parameters:
  enable-parallel-deployments: true

modules:

  - name: geo-weather-agent-srv
    type: python
    path: .
    parameters:
      buildpack: python_buildpack
      memory: 512M
      disk-quota: 1024M
      instances: 1
      command: python app/main.py
    properties:
      PORT: 8080
      CAP_SERVICE_URL: ~{cap-srv/srv-url}
    requires:
      - name: cap-srv
      - name: geo-weather-aicore
    provides:
      - name: geo-weather-agent
        properties:
          agent-url: ${default-url}

resources:
  - name: geo-weather-aicore
    type: org.cloudfoundry.managed-service
    parameters:
      service: aicore
      service-plan: extended
